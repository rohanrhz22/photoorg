# Packages FaceFind needs to detect & match faces (the face-recognition engine).
# The app runs the web UI without these, but matching stays disabled until they
# are installed. Everything runs locally; no image leaves your PC.
#
# Install with:
#   pip install -r requirements-vision.txt
#
numpy
opencv-contrib-python
Pillow
# Optional: enables HEIC/HEIF (iPhone) photo & selfie support.
pillow-heif
